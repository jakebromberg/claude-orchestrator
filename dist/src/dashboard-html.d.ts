/**
 * Renders a self-contained HTML dashboard page with inline CSS and JS.
 * The page connects to /api/events via SSE for live status updates.
 */
export declare function renderDashboardHtml(name: string): string;
